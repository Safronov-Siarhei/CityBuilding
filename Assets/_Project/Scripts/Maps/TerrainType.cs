namespace CityBuilder.Maps
{
    /// <summary>Terrain categories a pre-made map image is classified into. See MapImporter for the color palette.</summary>
    public enum TerrainType : byte
    {
        Grass = 0,
        Water = 1,
        Forest = 2,
        Stone = 3
    }
}
